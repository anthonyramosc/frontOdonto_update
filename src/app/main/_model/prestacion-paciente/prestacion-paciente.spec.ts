import { PrestacionPaciente } from './prestacion-paciente';

describe('PrestacionPaciente', () => {
  it('should create an instance', () => {
    expect(new PrestacionPaciente()).toBeTruthy();
  });
});
