import { PCalendarLocalDirective } from './pcalendar-local.directive';

describe('PCalendarLocalDirective', () => {
  it('should create an instance', () => {
    const directive = new PCalendarLocalDirective();
    expect(directive).toBeTruthy();
  });
});
